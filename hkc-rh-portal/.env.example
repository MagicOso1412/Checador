# URL base de hkc-backend. En desarrollo local, el default en código ya apunta
# a http://localhost:3000 — solo necesitas este archivo cuando el backend esté
# desplegado en el Mac mini (la URL pública del túnel/dominio).
VITE_API_URL=https://asistencia.tuempresa.com
